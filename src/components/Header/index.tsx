import React, { Fragment } from "react";
import { HeaderWrapper } from "./style";

const Header = () => {
  return (
    <Fragment>
      <HeaderWrapper />
    </Fragment>
  );
};

export default Header;
